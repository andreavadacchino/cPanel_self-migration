# Migration Platform V2

Use `AGENTS.md` as the entry point. Read the canonical conventions in `.ai/rules/` and the active work in `tasks/BACKLOG.md` before making changes. The Go CLI at the repository parent is reference material only; implement the V2 in FastAPI, Dramatiq, Python adapters, PostgreSQL, and React.

To execute backlog tasks autonomously, follow `.claude/TASK_EXECUTION_PROMPT.md`.
