# Compiled binary
/cpanel-self-migration

# Real config with credentials — never commit
/configs/host.yaml
# Same file when a run writes it into the working root (dogfooding) — never commit
/host.yaml

# Generated artifacts
*.log
/logs/

# Local diagnostic / audit reports — not part of the source
/diagnostics/
dns_verify_report.md
dogfood_giorginisposi/

# OS / editor / tooling noise
.DS_Store
/.tmp/
/.playwright-mcp/

# Run-artifacts written to the working root by tool runs (not source; root-anchored
# so tracked internal/testdata/* is never affected)
/acceptances.json
/events.jsonl
/job.json
/inventory_*.json
/inventory_*.md
/policy_report.json
/policy_report.md
/migration_checklist.json
/migration_checklist.md
/dns_import_plan.json
/dns_import_plan.md
